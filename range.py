# The range() function is used list of numbers.
#  the range(start, end, increment) // default start= 0 incr=1;

lst = list(range(11))
print(lst)
st = set(range(1, 11))
print(st)

# Sudhanshu vi9shwakarnna
